
      RelationshipsRequest:
        description: Request Body for Relationships
        content:
          application/json:
            schema:
              type: object
              required:
              - data
              properties:
                data:
                  type: object
                  properties:
                    contact:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Contact
                            associated with the Relationship. The keyword `null` is
                            not valid for this field.
                    description:
                      type: string
                      description: Describe the relationship between a Contact and
                        a Matter.
                    matter:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Matter associated
                            with the Relationship. The keyword `null` is not valid
                            for this field.
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
              - data
              properties:
                data:
                  type: object
                  properties:
                    contact:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Contact
                            associated with the Relationship. The keyword `null` is
                            not valid for this field.
                    description:
                      type: string
                      description: Describe the relationship between a Contact and
                        a Matter.
                    matter:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Matter associated
                            with the Relationship. The keyword `null` is not valid
                            for this field.
          multipart/form-data:
            schema:
              type: object
              required:
              - data
              properties:
                data:
                  type: object
                  properties:
                    contact:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Contact
                            associated with the Relationship. The keyword `null` is
                            not valid for this field.
                    description:
                      type: string
                      description: Describe the relationship between a Contact and
                        a Matter.
                    matter:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Matter associated
                            with the Relationship. The keyword `null` is not valid
                            for this field.
        required: false
