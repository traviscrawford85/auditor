
      TimersRequest:
        description: Request Body for Timers
        content:
          application/json:
            schema:
              type: object
              required:
              - data
              properties:
                data:
                  type: object
                  properties:
                    activity:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Activity
                            associated with the Timer. The keyword `null` is not valid
                            for this field.
                      required:
                      - id
                  required:
                  - activity
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
              - data
              properties:
                data:
                  type: object
                  properties:
                    activity:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Activity
                            associated with the Timer. The keyword `null` is not valid
                            for this field.
                      required:
                      - id
                  required:
                  - activity
          multipart/form-data:
            schema:
              type: object
              required:
              - data
              properties:
                data:
                  type: object
                  properties:
                    activity:
                      type: object
                      properties:
                        id:
                          type: integer
                          format: int64
                          description: The unique identifier for a single Activity
                            associated with the Timer. The keyword `null` is not valid
                            for this field.
                      required:
                      - id
                  required:
                  - activity
        required: false
