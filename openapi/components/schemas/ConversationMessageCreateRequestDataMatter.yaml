UnnamedSchema:
  description: ConversationMessageCreateRequestDataMatter
  properties:
    id:
      anyOf:
      - type: integer
      - type: 'null'
      default: null
      description: The unique identifier for a single Matter associated with the ConversationMessage.
        The keyword `null` is not valid for this field.
      title: Id
      type: integer
  title: ConversationMessageCreateRequestDataMatter
  type: object
